import { SesamienTypeColorPipe } from './sesamien-mention-color.pipe';

describe('SesamienTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new SesamienTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
