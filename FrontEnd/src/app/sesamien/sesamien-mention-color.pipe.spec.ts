import { SesamienMentionColorPipe } from './sesamien-mention-color.pipe';

describe('SesamienTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new SesamienMentionColorPipe();
    expect(pipe).toBeTruthy();
  });
});
